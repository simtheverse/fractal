# FPA-030: Diataxis Structure Implementation Gap

**Requirement:** FPA-030 — Documentation Structure (Diataxis)

**Status:** Partially resolved — top-level Diataxis directories exist and
`docs/explanation/` is populated. Remaining gaps below.

**Remaining gaps:**
1. `docs/tutorials/`, `docs/how-to/`, and `docs/reference/` are empty
2. Per-crate `docs/` directories use flat `docs/SPECIFICATION.md` rather than
   `docs/design/SPECIFICATION.md` as FPA-040 specifies
3. Per-crate Diataxis subdirectories do not exist

The prototype defers full structure to keep focus on architectural validation
rather than documentation completeness.
